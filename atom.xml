<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://24684623.github.io</id>
    <title>赵较瘦</title>
    <updated>2021-07-07T14:25:51.232Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://24684623.github.io"/>
    <link rel="self" href="https://24684623.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://24684623.github.io/images/avatar.png</logo>
    <icon>https://24684623.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 赵较瘦</rights>
    <entry>
        <title type="html"><![CDATA[栈和队列]]></title>
        <id>https://24684623.github.io/post/zhan-he-dui-lie/</id>
        <link href="https://24684623.github.io/post/zhan-he-dui-lie/">
        </link>
        <updated>2021-07-08T09:50:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="知识框架">【知识框架】</h3>
<figure data-type="image" tabindex="1"><img src="https://img-blog.csdnimg.cn/20210218195231660.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<h3 id="栈">栈</h3>
<h3 id="一-栈的基本概念">一、栈的基本概念</h3>
<h4 id="1-栈的定义">1、栈的定义</h4>
<p>栈（Stack）：是只允许在一端进行插入或删除的线性表。首先栈是一种线性表，但限定这种线性表只能在某一端进行插入和删除操作。</p>
<figure data-type="image" tabindex="2"><img src="https://img-blog.csdnimg.cn/20210218204524571.png#pic_center" alt="" loading="lazy"></figure>
<p><strong>栈顶（Top）</strong>：线性表允许进行插入删除的那一端。<br>
<strong>栈底（Bottom）</strong>：固定的，不允许进行插入和删除的另一端。<br>
<strong>空栈</strong>：不含任何元素的空表。</p>
<p>栈又称为后进先出（Last In First Out）的线性表，简称<strong>LIFO结构</strong></p>
<p>2、栈的常见基本操作</p>
<p>InitStack(&amp;S)：初始化一个空栈S。<br>
StackEmpty(S)：判断一个栈是否为空，若栈为空则返回true，否则返回false。<br>
Push(&amp;S, x)：进栈（栈的插入操作），若栈S未满，则将x加入使之成为新栈顶。<br>
Pop(&amp;S, &amp;x)：出栈（栈的删除操作），若栈S非空，则弹出栈顶元素，并用x返回。<br>
GetTop(S, &amp;x)：读栈顶元素，若栈S非空，则用x返回栈顶元素。<br>
DestroyStack(&amp;S)：栈销毁，并释放S占用的存储空间（“&amp;”表示引用调用）。<br>
二、栈的顺序存储结构</p>
<p>1、栈的顺序存储</p>
<p>采用顺序存储的栈称为顺序栈，它利用一组地址连续的存储单元存放自栈底到栈顶的数据元素，同时附设一个指针（top）指示当前栈顶元素的位置。<br>
若存储栈的长度为StackSize，则栈顶位置top必须小于StackSize。当栈存在一个元素时，top等于0，因此通常把空栈的判断条件定位top等于-1。<br>
栈的顺序存储结构可描述为：</p>
<pre><code class="language-java">#define MAXSIZE 50;  //定义栈中元素的最大个数
typedef int ElemType;   //ElemType的类型根据实际情况而定，这里假定为int
typedef struct{
    ElemType data[MAXSIZE];
    int top;    //用于栈顶指针
}SqStack;
</code></pre>
<p>若现在有一个栈，StackSize是5，则栈的普通情况、空栈、满栈的情况分别如下图所示：</p>
<figure data-type="image" tabindex="3"><img src="https://img-blog.csdnimg.cn/20210218225339549.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<h4 id="2-顺序栈的基本算法">2、顺序栈的基本算法</h4>
<h5 id="1初始化">（1）初始化</h5>
<pre><code class="language-java">void InitStack(SqStack *S){
    S-&gt;top = -1;    //初始化栈顶指针
}
</code></pre>
<h5 id="2判栈空">2）判栈空</h5>
<pre><code class="language-java">bool StackEmpty(SqStack S){
    if(S.top == -1){    
        return true;    //栈空
    }else{  
        return false;   //不空
    }
}
</code></pre>
<h5 id="3进栈">（3）进栈</h5>
<figure data-type="image" tabindex="4"><img src="https://img-blog.csdnimg.cn/20210218231217842.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<p>进栈操作push，代码如下：</p>
<pre><code class="language-java">/*插入元素e为新的栈顶元素*/
Status Push(SqStack *S, ElemType e){
    //满栈
    if(S-&gt;top == MAXSIZE-1){
        return ERROR;
    }
    S-&gt;top++;   //栈顶指针增加一
    S-&gt;data[S-&gt;top] = e;    //将新插入元素赋值给栈顶空间
    return OK;
}
</code></pre>
<h5 id="4出栈">（4）出栈</h5>
<p>出栈操作pop，代码如下：</p>
<pre><code class="language-java">/*若栈不空，则删除S的栈顶元素，用e返回其值，并返回OK；否则返回ERROR*/
Status Pop(SqStack *S, ElemType *e){
    if(S-&gt;top == -1){
        return ERROR;
    }
    *e = S-&gt;data[S-&gt;top];   //将要删除的栈顶元素赋值给e
    S-&gt;top--;   //栈顶指针减一
    return OK;
}
</code></pre>
<h5 id="5读栈顶元素">（5）读栈顶元素</h5>
<pre><code class="language-java">/*读栈顶元素*/
Status GetTop(SqStack S, ElemType *e){
    if(S-&gt;top == -1){   //栈空
        return ERROR;
    }
    *e = S-&gt;data[S-&gt;top];   //记录栈顶元素
    return OK;
}
3、共享栈（两栈共享空间）
</code></pre>
<p><strong>（1）共享栈概念</strong></p>
<p>利用栈底位置相对不变的特征，可让两个顺序栈共享一个一维数组空间，将两个栈的栈底分别设置在共享空间的两端，两个栈顶向共享空间的中间延伸，如下图所示：</p>
<figure data-type="image" tabindex="5"><img src="https://img-blog.csdnimg.cn/2021021909353762.png#pic_center" alt="" loading="lazy"></figure>
<p>两个栈的栈顶指针都指向栈顶元素，top0=-1时0号栈为空，top1=MaxSize时1号栈为空；仅当两个栈顶指针相邻（top0+1=top1）时，判断为栈满。当0号栈进栈时top0先加1再赋值，1号栈进栈时top1先减一再赋值出栈时则刚好相反。</p>
<p><strong>（2）共享栈的空间结构</strong></p>
<p>代码如下：</p>
<pre><code class="language-java">/*两栈共享空间结构*/
#define MAXSIZE 50;  //定义栈中元素的最大个数
typedef int ElemType;   //ElemType的类型根据实际情况而定，这里假定为int
/*两栈共享空间结构*/
typedef struct{
	ElemType data[MAXSIZE];
	int top0;	//栈0栈顶指针
	int top1;	//栈1栈顶指针
}SqDoubleStack;

</code></pre>
<p><strong>（3）共享栈进栈</strong></p>
<p>对于两栈共享空间的push方法，我们除了要插入元素值参数外，还需要有一个判断是栈0还是栈1的栈号参数stackNumber。<br>
共享栈进栈的代码如下：</p>
<pre><code class="language-java">/*插入元素e为新的栈顶元素*/
Status Push(SqDoubleStack *S, Elemtype e, int stackNumber){
    if(S-&gt;top0+1 == S-&gt;top1){   //栈满
        return ERROR;
    }
    if(stackNumber == 0){   //栈0有元素进栈
        S-&gt;data[++S-&gt;top0] = e; //若栈0则先top0+1后给数组元素赋值
    }else if(satckNumber == 1){ //栈1有元素进栈
        S-&gt;data[--S-&gt;top1] = e; //若栈1则先top1-1后给数组元素赋值
    }
    return OK;
}
</code></pre>
<p><strong>（4）共享栈出栈</strong></p>
<p>代码如下：</p>
<pre><code class="language-java">/*若栈不空，则删除S的栈顶元素，用e返回其值，并返回OK；否则返回ERROR*/
Status Pop(SqDoubleStack *S, ElemType *e, int stackNumber){
    if(stackNumber == 0){
        if(S-&gt;top0 == -1){
            return ERROR;   //说明栈0已经是空栈，溢出
        }
        *e = S-&gt;data[S-&gt;top0--]; //将栈0的栈顶元素出栈，随后栈顶指针减1
    }else if(stackNumber == 1){
        if(S-&gt;top1 == MAXSIZE){
            return ERROR;   //说明栈1是空栈，溢出
        }
        *e = S-&gt;data[S-&gt;top1++];    //将栈1的栈顶元素出栈，随后栈顶指针加1
    }
    return OK;
}
</code></pre>
<h4 id="三-栈的链式存储结构">三、栈的链式存储结构</h4>
<h5 id="1-链栈">1、链栈</h5>
<p>采用链式存储的栈称为链栈，链栈的优点是便于多个栈共享存储空间和提高其效率，且不存在栈满上溢的情况。通常采用单链表实现，并规定所有操作都是在单链表的表头进行的。这里规定链栈没有头节点，Lhead指向栈顶元素，如下图所示。</p>
<figure data-type="image" tabindex="6"><img src="https://img-blog.csdnimg.cn/2021021910502017.png#pic_center" alt="" loading="lazy"></figure>
<p>对于空栈来说，链表原定义是头指针指向空，那么链栈的空其实就是top=NULL的时候。</p>
<p>链栈的结构代码如下：</p>
<pre><code class="language-java">/*栈的链式存储结构*/
/*构造节点*/
typedef struct StackNode{
    ElemType data;
    struct StackNode *next;
}StackNode, *LinkStackPrt;
/*构造链栈*/
typedef struct LinkStack{
    LinkStackPrt top;
    int count;
}LinkStack;
</code></pre>
<h5 id="2-链栈的基本算法">2、链栈的基本算法</h5>
<h5 id="1链栈的进栈">（1）链栈的进栈</h5>
<p>对于链栈的进栈push操作，假设元素值为e的新节点是s，top为栈顶指针，示意图如下：</p>
<figure data-type="image" tabindex="7"><img src="https://img-blog.csdnimg.cn/20210524215216496.png#pic_center" alt="" loading="lazy"></figure>
<p>代码如下：</p>
<pre><code class="language-java">/*插入元素e为新的栈顶元素*/
Status Push(LinkStack *S, ElemType e){
    LinkStackPrt p = (LinkStackPrt)malloc(sizeof(StackNode));
    p-&gt;data = e;
    p-&gt;next = S-&gt;top;    //把当前的栈顶元素赋值给新节点的直接后继
    S-&gt;top = p; //将新的结点S赋值给栈顶指针
    S-&gt;count++;
    return OK;
}
</code></pre>
<p><strong>（2）链栈的出栈</strong></p>
<p>链栈的出栈pop操作，也是很简单的三句操作。假设变量p用来存储要删除的栈顶结点，将栈顶指针下移以为，最后释放p即可，如下图所示：</p>
<figure data-type="image" tabindex="8"><img src="https://img-blog.csdnimg.cn/20210219110743130.png#pic_center" alt="" loading="lazy"></figure>
<p>代码如下：</p>
<pre><code class="language-java">/*若栈不空，则删除S的栈顶元素，用e返回其值，并返回OK；否则返回ERROR*/
Status Pop(LinkStack *S, ElemType *e){
    LinkStackPtr p;
    if(StackEmpty(*S)){
        return ERROR;
    }
    *e = S-&gt;top-&gt;data;
    p = S-&gt;top; //将栈顶结点赋值给p
    S-&gt;top = S-&gt;top-&gt;next;  //使得栈顶指针下移一位，指向后一结点
    free(p);    //释放结点p
    S-&gt;count--;
    return OK;
}
</code></pre>
<h5 id="3-性能分析">3、性能分析</h5>
<p>链栈的进栈push和出栈pop操作都很简单，时间复杂度均为O(1)。<br>
对比一下顺序栈与链栈,它们在时间复杂度上是一样的,均为O(1)。对于空间性能,顺序栈需要事先确定一个固定的长度,可能会存在内存空间浪费的问题,但它的优势是存取时定位很方便,而链栈则要求每个元素都有指针域,这同时也增加了一些内存开销,但对于栈的长度无限制。所以它们的区别和线性表中讨论的一样,如果栈的使用过程中元素变化不可预料,有时很小,有时非常大,那么最好是用链栈,反之,如果它的变化在可控范围内,建议使用顺序栈会更好一些。</p>
<h4 id="四-栈的应用递归">四、栈的应用——递归</h4>
<h5 id="1-递归的定义">1、递归的定义</h5>
<p>递归是一种重要的程序设计方法。简单地说,若在一个函数、过程或数据结构的定义中又应用了它自身,则这个函数、过程或数据结构称为是递归定义的,简称递归。<br>
它通常把一个大型的复杂问题层层转化为一个与原问题相似的规模较小的问题来求解,递归策略只需少量的代码就可以描述岀解题过程所需要的多次重复计算,大大减少了程序的代码量但在通常情况下,它的效率并不是太高。</p>
<h5 id="2-斐波那契数列">2、斐波那契数列</h5>
<p>在解释斐波那契数列之前，我们想看经典的兔子繁殖的问题：</p>
<p>说如果兔子在出生两个月后,就有繁殖能力,一对兔子每个月能生出一对小兔子 来。假设所有兔都不死,那么一年以后可以繁殖多少对兔子呢?<br>
第一个月初有一对刚诞生的兔子<br>
第二个月之后（第三个月初）它们可以生育<br>
每月每对可生育的兔子会诞生下一对新兔子<br>
兔子永不死去<br>
我们拿新出生的一对小兔子分析一下:第一个月小兔子没有繁殖能力,所以还是一对;两个月后,生下一对小兔子数共有两对;三个月以后,老兔子又生下一对,因为小兔子还没有繁殖能力,所以一共是三对…依次类推得出这样一个图：</p>
<figure data-type="image" tabindex="9"><img src="https://img-blog.csdnimg.cn/20210219113201879.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<p>从这个图可以看出，斐波那契数列数列有一个明显的特点，即：前面两项之和，构成了后一项。<br>
如果用数学函数定义斐波那契数列，那就是：</p>
<figure data-type="image" tabindex="10"><img src="https://img-blog.csdnimg.cn/20210219114402348.png#pic_center" alt="" loading="lazy"></figure>
<p>而这个，就是递归的一个典型例子，用程序实现时如下：</p>
<pre><code class="language-java">/*斐波那契数列的实现*/
int Fib(int n){
    if(n == 0){
        return 0;   //边界条件
    }else if(n == 1){
        return 1;	//边界条件
    }else{
        return Fib(n-1) + Fib(n-2); //递归表达式
    }
}
</code></pre>
<p>必须注意递归模型不能是循环定义的,其必须满足下面的两个条件</p>
<p>递归表达式(递归体)<br>
边界条件(递归出口)<br>
递归的精髓在于能否将原始问题转换为属性相同但规模较小的问题<br>
在递归调用的过程中,系统为每一层的返回点、局部变量、传入实参等开辟了递归工作栈来进行数据存储,递归次数过多容易造成栈溢出等。而其效率不高的原因是递归调用过程中包含很多重复的计算。下面以n=5为例,列出递归调用执行过程,如图所示：</p>
<figure data-type="image" tabindex="11"><img src="https://img-blog.csdnimg.cn/20210219115532873.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<p>如图可知，程序每往下递归一次，就会把运算结果放到栈中保存，直到程序执行到临界条件，然后便会把保存在栈中的值按照先进后出的顺序一个个返回，最终得出结果。</p>
<h3 id="五-栈的应用四则运算表达式求值">五、栈的应用——四则运算表达式求值</h3>
<h4 id="1-后缀表达式计算结果">1、后缀表达式计算结果</h4>
<p>表达式求值是程序设计语言编译中一个最基本的问题,它的实现是栈应用的一个典型范例。中缀表达式不仅依赖运算符的优先级,而且还要处理括号。后缀表达式的运算符在操作数后面，在后缀表达式中已考虑了运算符的优先级,没有括号,只有操作数和运算符。例如中缀表达式 A + B ∗ ( C − D ) − E / F A+B*(C-D)-E/F A+B∗(C−D)−E/F所对应的后缀表达式为 A B C D − ∗ + E F / − ABCD-*+EF/- ABCD−∗+EF/−。</p>
<p>后缀表达式计算规则：从左到右遍历表达式的每个数字和符号，遇到是数字就进栈，遇到是符号，就将处于栈顶两个数字出栈，进项运算，运算结果进栈，一直到最终获得结果。<br>
后缀表达式 A B C D − ∗ + E F / − ABCD-*+EF/- ABCD−∗+EF/−求值的过程需要12步，如下表所示：</p>
<figure data-type="image" tabindex="12"><img src="https://img-blog.csdnimg.cn/20210219121121556.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<p>读者也可将后缀表达式与原运算式对应的表达式树(用来表示算术表达式的二元树)的后序遍历进行比较,可以发现它们有异曲同工之妙。<br>
如下图则是 A + B ∗ ( C − D ) − E / F A+B*(C-D)-E/F A+B∗(C−D)−E/F对应的表达式，它的后序遍历即是表达式 A B C D − ∗ + E F / − ABCD-*+EF/- ABCD−∗+EF/−。</p>
<figure data-type="image" tabindex="13"><img src="https://img-blog.csdnimg.cn/20210219121509722.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<h4 id="2-中缀表达式转后缀表达式">2、中缀表达式转后缀表达式</h4>
<p>我们把平时所用的标准四则运算表达式,即 a + b − a ∗ ( ( c + d ) / e − f ) + g a+b-a*((c+d)/e-f)+g a+b−a∗((c+d)/e−f)+g叫做中缀<br>
表达式。因为所有的运算符号都在两数字的中间,现在我们的问题就是中缀到后缀的转化。</p>
<p>规则:从左到右遍历中缀表达式的每个数字和符号,若是数字就输出,即成为后<br>
缀表达式的一部分;若是符号,则判断其与栈顶符号的优先级,是右括号或优先级低于栈顶符号(乘除优先加减)则栈顶元素依次出栈并输出,并将当前符号进栈,一直到最终输出后缀表达式为止。<br>
例：将中缀表达式 a + b − a ∗ ( ( c + d ) / e − f ) + g a+b-a*((c+d)/e-f)+g a+b−a∗((c+d)/e−f)+g转化为相应的后缀表达式。</p>
<p>分析：需要根据操作符</p>
<p>的优先级来进行栈的变化,我们用icp来表示当前扫描到的运算符ch的优先级,该运算符进栈后的优先级为isp,则运算符的优先级如下表所示[isp是栈内优先( in stack priority)数,icp是栈外优先( in coming priority)数]。</p>
<figure data-type="image" tabindex="14"><img src="https://img-blog.csdnimg.cn/20210219123715333.png#pic_center" alt="" loading="lazy"></figure>
<p>我们在表达式后面加上符号‘#’,表示表达式结束。具体转换过程如下：</p>
<figure data-type="image" tabindex="15"><img src="https://img-blog.csdnimg.cn/20210219123919219.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<p>即相应的后缀表达式为 a b + a c d + e / f − ∗ − g + ab+acd+e/f-*-g+ ab+acd+e/f−∗−g+。</p>
<p>队列</p>
<h3 id="一-队列的基本概念">一、队列的基本概念</h3>
<h4 id="1-队列的定义">1、队列的定义</h4>
<p>队列（queue）是只允许在一端进行插入操作，而在另一端进行删除操作的线性表。<br>
队列是一种先进先出（First In First Out）的线性表，简称FIFO。允许插入的一端称为队尾，允许删除的一端称为队头。</p>
<figure data-type="image" tabindex="16"><img src="https://img-blog.csdnimg.cn/20210219144731888.png#pic_center" alt="" loading="lazy"></figure>
<p>队头（Front）：允许删除的一端，又称队首。<br>
队尾（Rear）：允许插入的一端。<br>
空队列：不包含任何元素的空表。</p>
<h4 id="2-队列的常见基本操作">2、队列的常见基本操作</h4>
<p>InitQueue(&amp;Q)：初始化队列，构造一个空队列Q。<br>
QueueEmpty(Q)：判队列空，若队列Q为空返回true，否则返回false。<br>
EnQueue(&amp;Q, x)：入队，若队列Q未满，将x加入，使之成为新的队尾。<br>
DeQueue(&amp;Q, &amp;x)：出队，若队列Q非空，删除队头元素，并用x返回。<br>
GetHead(Q, &amp;x)：读队头元素，若队列Q非空，则将队头元素赋值给x。</p>
<h5 id="二-队列的顺序存储结构">二、队列的顺序存储结构</h5>
<p>队列的顺序实现是指分配一块连续的存储单元存放队列中的元素，并附设两个指针：队头指针 front指向队头元素，队尾指针 rear 指向队尾元素的下一个位置。</p>
<h4 id="1-顺序队列">1、顺序队列</h4>
<p>队列的顺序存储类型可描述为:</p>
<pre><code class="language-java">#define MAXSIZE 50;	//定义队列中元素的最大个数
typedef struct{
	ElemType data[MAXSIZE];	//存放队列元素
	int front,rear;
}SqQueue;
</code></pre>
<p>初始状态（队空条件）：Q-&gt;front == Q-&gt;rear == 0。<br>
进队操作：队不满时，先送值到队尾元素，再将队尾指针加1。<br>
出队操作：队不空时，先取队头元素值，再将队头指针加1。</p>
<figure data-type="image" tabindex="17"><img src="https://img-blog.csdnimg.cn/20210219152051156.png#pic_center" alt="" loading="lazy"></figure>
<p>如图d，队列出现“上溢出”，然而却又不是真正的溢出，所以是一种“假溢出”。</p>
<h4 id="2-循环队列">2、循环队列</h4>
<p>解决假溢出的方法就是后面满了，就再从头开始，也就是头尾相接的循环。我们把队列的这种头尾相接的顺序存储结构称为循环队列。<br>
当队首指针Q-&gt;front = MAXSIZE-1后，再前进一个位置就自动到0，这可以利用除法取余运算（%）来实现。</p>
<p>初始时：Q-&gt;front = Q-&gt;rear=0。<br>
队首指针进1：Q-&gt;front = (Q-&gt;front + 1) % MAXSIZE。<br>
队尾指针进1：Q-&gt;rear = (Q-&gt;rear + 1) % MAXSIZE。<br>
队列长度：(Q-&gt;rear - Q-&gt;front + MAXSIZE) % MAXSIZE。<br>
出队入队时，指针都按照顺时针方向前进1，如下图所示：</p>
<figure data-type="image" tabindex="18"><img src="https://img-blog.csdnimg.cn/2021021919300137.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<p>那么，循环队列队空和队满的判断条件是什么呢？<br>
显然，队空的条件是 Q-&gt;front == Q-&gt;rear 。若入队元素的速度快于出队元素的速度，则队尾指针很快就会赶上队首指针，如图( d1 ）所示，此时可以看出队满时也有 Q -&gt;front == Q -&gt; rear 。<br>
为了区分队空还是队满的情况，有三种处理方式：<br>
（1）牺牲一个单元来区分队空和队满，入队时少用一个队列单元，这是种较为普遍的做法，约定以“队头指针在队尾指针的下一位置作为队满的标志”，如图 ( d2 ）所示。</p>
<p>队满条件： (Q-&gt;rear + 1)%Maxsize == Q-&gt;front<br>
队空条件仍： Q-&gt;front == Q-&gt;rear<br>
队列中元素的个数： (Q-&gt;rear - Q -&gt;front + Maxsize)% Maxsize<br>
（2）类型中增设表示元素个数的数据成员。这样，队空的条件为 Q-&gt;size == O ；队满的条件为 Q-&gt;size == Maxsize 。这两种情况都有 Q-&gt;front == Q-&gt;rear<br>
（3）类型中增设tag 数据成员，以区分是队满还是队空。tag 等于0时，若因删除导致 Q-&gt;front == Q-&gt;rear ，则为队空；tag 等于 1 时，若因插入导致 Q -&gt;front == Q-&gt;rear ，则为队满。</p>
<p>我们重点讨论第一种方法</p>
<h4 id="3-循环队列常见基本算法">3、循环队列常见基本算法</h4>
<h5 id="1循环队列的顺序存储结构">（1）循环队列的顺序存储结构</h5>
<pre><code class="language-java">typedef int ElemType;   //ElemType的类型根据实际情况而定，这里假定为int
#define MAXSIZE 50;  //定义元素的最大个数
/*循环队列的顺序存储结构*/
typedef struct{
    ElemType data[MAXSIZE];
    int front;  //头指针
    int rear;   //尾指针,若队列不空，指向队列尾元素的下一个位置
}SqQueue;
</code></pre>
<h5 id="2循环队列的初始化">（2）循环队列的初始化</h5>
<pre><code class="language-java">/*初始化一个空队列Q*/
Status InitQueue(SqQueue *Q){
    Q-&gt;front = 0;
    Q-&gt;rear = 0;
    return OK;
}
</code></pre>
<h5 id="3循环队列判队空">（3）循环队列判队空</h5>
<pre><code class="language-java">/*判队空*/
bool isEmpty(SqQueue Q){
    if(Q.rear == Q.front){
        return true;
    }else{
        return false;
    }
}
</code></pre>
<h5 id="4求循环队列长度">（4）求循环队列长度</h5>
<pre><code class="language-java">/*返回Q的元素个数，也就是队列的当前长度*/
int QueueLength(SqQueue Q){
    return (Q.rear - Q.front + MAXSIZE) % MAXSIZE;
}
</code></pre>
<h5 id="5循环队列入队">（5）循环队列入队</h5>
<pre><code class="language-java">/*若队列未满，则插入元素e为Q新的队尾元素*/
Status EnQueue(SqQueue *Q, ElemType e){
    if((Q-&gt;real + 1) % MAXSIZE == Q-&gt;front){
        return ERROR;   //队满
    }
    Q-&gt;data[Q-&gt;rear] = e;   //将元素e赋值给队尾
    Q-&gt;rear = (Q-&gt;rear + 1) % MAXSIZE;  //rear指针向后移一位置，若到最后则转到数组头部
    return OK;
}
</code></pre>
<h5 id="6循环队列出队">（6）循环队列出队</h5>
<pre><code class="language-java">/*若队列不空，则删除Q中队头元素，用e返回其值*/
Status DeQueue(SqQueue *Q, ElemType *e){
    if(isEmpty(Q)){
        return REEOR;   //队列空的判断
    }
    *e = Q-&gt;data[Q-&gt;front]; //将队头元素赋值给e
    Q-&gt;front = (Q-&gt;front + 1) % MAXSIZE;    //front指针向后移一位置，若到最后则转到数组头部
}

#### 
</code></pre>
<h4 id="三-队列的链式存储结构">三、队列的链式存储结构</h4>
<h5 id="1-链队列">1、链队列</h5>
<p>队列的链式存储结构表示为链队列，它实际上是一个同时带有队头指针和队尾指针的单链表，只不过它只能尾进头出而已。</p>
<figure data-type="image" tabindex="19"><img src="https://img-blog.csdnimg.cn/20210219202056884.png#pic_center" alt="" loading="lazy"></figure>
<p>空队列时，front和real都指向头结点。</p>
<figure data-type="image" tabindex="20"><img src="https://img-blog.csdnimg.cn/20210219202141927.png#pic_center" alt="" loading="lazy"></figure>
<p>2、链队列常见基本算法</p>
<h5 id="1链队列存储类型">（1）链队列存储类型</h5>
<pre><code class="language-java">/*链式队列结点*/
typedef struct {
	ElemType data;
	struct LinkNode *next;
}LinkNode;
/*链式队列*/
typedef struct{
	LinkNode *front, *rear;	//队列的队头和队尾指针
}LinkQueue;
</code></pre>
<p>当Q-&gt;front == NULL 并且 Q-&gt;rear == NULL 时，链队列为空。</p>
<h5 id="2链队列初始化">（2）链队列初始化</h5>
<figure data-type="image" tabindex="21"><img src="https://img-blog.csdnimg.cn/20210219203226794.png#pic_center" alt="" loading="lazy"></figure>
<pre><code class="language-java">void InitQueue(LinkQueue *Q){
	Q-&gt;front = Q-&gt;rear = (LinkNode)malloc(sizeof(LinkNode));	//建立头结点
	Q-&gt;front-&gt;next = NULL;	//初始为空
}
</code></pre>
<h5 id="3链队列入队">（3）链队列入队</h5>
<figure data-type="image" tabindex="22"><img src="https://img-blog.csdnimg.cn/20210219204420535.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1JlYWxfRm9vbF8=,size_16,color_FFFFFF,t_70#pic_center" alt="" loading="lazy"></figure>
<pre><code class="language-java">Status EnQueue(LinkQueue *Q, ElemType e){
	LinkNode s = (LinkNode)malloc(sizeof(LinkNode));
	s-&gt;data = e;
	s-&gt;next = NULL;
	Q-&gt;rear-&gt;next = s;	//把拥有元素e新结点s赋值给原队尾结点的后继
	Q-&gt;rear = s;	//把当前的s设置为新的队尾结点
	return OK;
}

</code></pre>
<p>（4）链队列出队</p>
<p>出队操作时，就是头结点的后继结点出队，将头结点的后继改为它后面的结点，若链表除头结点外只剩一个元素时，则需将rear指向头结点。</p>
<figure data-type="image" tabindex="23"><img src="https://img-blog.csdnimg.cn/20210219204716316.png#pic_center" alt="" loading="lazy"></figure>
<pre><code class="language-java">/*若队列不空，删除Q的队头元素，用e返回其值，并返回OK，否则返回ERROR*/
Status DeQueue(LinkQueue *Q, Elemtype *e){
	LinkNode p;
	if(Q-&gt;front == Q-&lt;rear){
		return ERROR;
	}
	p = Q-&gt;front-&gt;next;	//将欲删除的队头结点暂存给p
	*e = p-&gt;data;	//将欲删除的队头结点的值赋值给e
	Q-&gt;front-&gt;next = p-&gt;next;	//将原队头结点的后继赋值给头结点后继
	//若删除的队头是队尾，则删除后将rear指向头结点
	if(Q-&gt;rear == p){	
		Q-&gt;rear = Q-&gt;front;
	}
	free(p);
	return OK;
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[线性表]]></title>
        <id>https://24684623.github.io/post/xian-xing-biao/</id>
        <link href="https://24684623.github.io/post/xian-xing-biao/">
        </link>
        <updated>2021-07-06T16:23:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="线性表">线性表</h2>
<p>线性表是最基本、最简单、也是最常用的一种数据结构。线性表*（linear list）*是<a href="https://baike.baidu.com/item/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/1450">数据结构</a>的一种，一个线性表是n个（n&gt;=0)<strong>具有相同特性</strong>(同一数据对象)的数据元素的<strong>有限</strong>序列。</p>
<h6 id="线性表有空线性表n0和非空线性表n0">线性表有空线性表(n=0)和非空线性表（n&gt;0)</h6>
<h3 id="非空线性表n0特点">非空线性表（n&gt;0)特点：</h3>
<p>1．集合中必存在唯一的一个“第一元素”。</p>
<p>2．集合中必存在唯一的一个 “最后元素” 。</p>
<p>3．除最后一个元素之外，均有唯一的后继(后件)。</p>
<p>4．除第一个元素之外，均有唯一的前驱(前件)。</p>
<h3 id="线性表-包括顺序表和链表">线性表、包括顺序表和链表，</h3>
<h6 id="顺序表里面元素的地址是连续的-链表里面节点的地址不是连续的是通过指针连起来的">顺序表里面元素的地址是连续的。链表里面节点的地址不是连续的，是通过指针连起来的。</h6>
<h4 id="1顺序存储">1．顺序存储</h4>
<p>顺序表，使用数组实现，一组地址连续的存储单元，数组大小有两种方式指定，一是静态分配，二是动态扩展。</p>
<p>注：线性表从1开始，而数组从0开始。</p>
<p>优点：随机访问特性，查找O(1)时间，存储密度高；逻辑上相邻的元素，物理上也相邻；</p>
<p>缺点：插入删除需移动大量元素。</p>
<p>顺序表相关的操作跟数组有关，一般都是移动数组元素。</p>
<p>这里说一下插入和删除时的边界条件，首先线性表从1开始，数组从0开始，单纯的文件说明不够直接，来看图说话吧。</p>
<figure data-type="image" tabindex="1"><img src="https://images2015.cnblogs.com/blog/941605/201605/941605-20160511073924452-854629572.png" alt="" loading="lazy"></figure>
<p>插入时：对于线性表来说最小能插入的位置是1，最大能插入的位置是8（=7+1），所以  1&lt;= index &lt;=(7+1)；移动数组元素时要注意，for (int i = count; i &gt;= index; i--) {  items[i] = items[i-1];}</p>
<p>删除时：只能在蓝色方块之间寻找节点删除，即1 &lt;= index &lt;= 7。移动元素，for (i = index; i &lt; count; i++) { items[i-1] = items[i];}</p>
<h5 id="2链式存储">2．链式存储</h5>
<p>链表的定义是递归的，它或者为空null，或者指向另一个节点node的引用，这个节点含有下一个节点或链表的引用。</p>
<p>与顺序存储相比，允许存储空间不连续，插入删除时不需要移动大量的元素，只需修改指针即可，但查找某个元素，只能从头遍历整个链表。</p>
<p>Java中使用嵌套类来定义节点的抽象数据类型：</p>
<pre><code class="language-java">private class Node{
    // 链表节点的嵌套类
    T item; // 节点内容
    Node next; // 后继节点
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://images2015.cnblogs.com/blog/941605/201605/941605-20160511074135202-251898237.png" alt="" loading="lazy"></figure>
<p><strong>2.1 单链表</strong></p>
<p>使用任意存储单元来存储线性表中的数据元素，节点类型如上。</p>
<p>单链表分为带头结点和不带头结点两种，不管有没有头结点，头指针都指向链表的第一个节点（有头结点指向头结点）。</p>
<p>头结点：数值域可不设任何信息，头结点的指针域指向链表的第一个元素。</p>
<p>带头节点的好处有：</p>
<p>（1）链表第一位置节点上的操作和其它位置上的操作一致</p>
<p>（2）无论链表是否为空，头指针都指向头结点（非空），空表和非空表处理一样</p>
<p>（这里我没有使用头结点）</p>
<p>注：链表麻烦的地方是插入和删除时指针的修改，保证不断链，一般先断后链。</p>
<figure data-type="image" tabindex="3"><img src="https://images2015.cnblogs.com/blog/941605/201605/941605-20160511074219984-621617430.png" alt="" loading="lazy"></figure>
<p>基本操作</p>
<ol>
<li>
<h5 id="头插法">头插法</h5>
</li>
</ol>
<p>将新节点插入到当前链表的表头，（头结点之后），插入的顺序与链表中的顺序相反，关键点就是记住旧的表头，生成一个新的放到旧表头前面，如图：</p>
<figure data-type="image" tabindex="4"><img src="https://images2015.cnblogs.com/blog/941605/201605/941605-20160511074306015-588143690.png" alt="" loading="lazy"></figure>
<p><strong>核心代码：</strong></p>
<pre><code class="language-java">public void headInsert(T item) {
    Node old = first;
    first = new Node();
    first.item = item;
    first.next = old;
    count++;
}

2. 
</code></pre>
<ol start="2">
<li>
<h5 id="尾插法">尾插法</h5>
</li>
</ol>
<p>增加一个尾指针，新节点插到链表的尾部，插入的顺序和链表的顺序一致，如图：</p>
<figure data-type="image" tabindex="5"><img src="https://images2015.cnblogs.com/blog/941605/201605/941605-20160511074306015-588143690.png" alt="" loading="lazy"></figure>
<p>核心代码：</p>
<pre><code class="language-java">public void tailInsert(T item) {
    Node old = last;
    last = new Node();
    last.item = item;
    last.next = null;
    if (isEmpty()) {
        first = last;
    } else {
        old.next = last;
    }
    count++;
}
</code></pre>
<p>节点的插入和删除，要点是先断后连，关键就是不要断链了，以插入为例（把s插入p和q之间），先断意思是先把p-&gt;q断了，变成s-&gt;q，后连，最后再把p和s连接起来。</p>
<ol start="3">
<li>
<h5 id="插入节点">插入节点</h5>
</li>
</ol>
<p>待插入节点为s，一般采用后插法，即先找到插入位置节点的前驱节点，然后插入，时间复杂度O(n)。</p>
<p>核心代码为：</p>
<pre><code class="language-java">p=getNodeByIndex(i-1);
s.next = p.next;
p.next = s;
</code></pre>
<p>还有一种方法是，直接插入到位置的后面（前插法），然后交换两个节点的值，插入的节点到了指定位置，时间复杂度O(1)：</p>
<p>核心代码：</p>
<pre><code class="language-java">s.next = p.next;
p.next = s;
temp = p.item;    // 交换内容
p.item = s.item;
s.item = temp;
</code></pre>
<ol start="4">
<li>
<h5 id="删除节点">删除节点</h5>
</li>
</ol>
<p>待删除节点为q，也是先找到前驱节点，修改指针域即可，时间复杂度O(n)。</p>
<p>核心代码：</p>
<pre><code class="language-java">P = getNodeByIndex(i-1);
q = p.next;
p.next = q.next;
q = null;
</code></pre>
<p>删除节点也能直接删除其后继节点，然后将后继节点的内容赋给自己即可，时间复杂度为O(1)：</p>
<p>核心代码：</p>
<pre><code class="language-java">q = p.next;
p.item = p.next.item;
p.next = q.next;
q = null;
</code></pre>
<h5 id="22-双链表">2.2 双链表</h5>
<p>单链表节点的缺点是只有一个后继节点，访问前驱节点只能从头遍历（如插入、删除），时间复杂度为O(n)。双链表，即添加一个指向前驱的节点，节点类型如下：</p>
<pre><code class="language-java">private class Node{
    // 链表节点的嵌套类
    T item; // 节点内容
    Node prior, next; // 前驱节点和后继节点
}
</code></pre>
<p>双链表的查找和单链表的相同再次不在赘述，双链表的构造也分为头插和尾插，与单链表唯一不同的是修改前驱指针prior，具体见源码。插入和删除时不同，因为需要修改两个指针，如果给定要操作的节点，插入和删除的时间复杂度为O(1)。</p>
<p>注：插入删除操作同样也是先断后连。</p>
<p><strong>1.插入节点</strong></p>
<p>在p节点后插入s节点，先断后连，先把p和原后继节点的链条给断了，使后继节点只跟s节点有关：</p>
<pre><code class="language-java">①s.next = p.next; // 先断了p的后继
②p.next.prior = s; // 在断了p后继的前驱
③s.prior = p; // 让s的前驱指向p
④p.next = s; // p的后继指向s，重新连接上链条，此步必须在①②之后
</code></pre>
<p><strong>2.删除节点</strong></p>
<p>删除节点p的后继节点q，也是先断后连，把q和其后继节点的关系，转让给p即可：</p>
<pre><code class="language-java">①p.next = q.next; // 先断了q的后继
②q.next.prior = p; // 在断了q后继的前驱

删除节点q的前驱节点p，把p和去前驱节点的关系转让给q即可：
①q = p.prior.next; // 把p前驱节点的后继改成q
②q.prior = p.prior; // 把q的前驱节点改成p的前驱节点**2.3 循环链表**
</code></pre>
<p><strong>1.循环单链表</strong></p>
<p>与单链表的区别在于，表中最后一个节点的指针不为null，而改为指向头结点（第一个节点），从而整个链表形成一个环。判断循环单链表是否为空，判断是否等于头指针。</p>
<p>只有一个尾指针的循环单例表，可以很方便的操作表头和表尾，因为尾指针的后继就是头指针O(1) 。</p>
<p><strong>1.循环双链表</strong></p>
<p>与双链表的区别在于，头结点的prior指针指向尾节点，尾节点的next指针指向头结点。</p>
<h5 id="24-静态链表">2.4 静态链表</h5>
<p>静态链表是借助数组来描述线性表的链式存储结构，节点也有数据域和指针域，这里的指针是节点的相对地址（数组下标），也需要预先分配一块连续的内存空间。</p>
<p>特点，插入删除和动态链表一样，以next==-1为结束标志。</p>
<p>2.5 顺序表和链表的比较</p>
<ol>
<li>
<p>顺序表可以顺序存取，也支持随机存取；链表只能顺序存取。</p>
</li>
<li>
<p>顺序表逻辑上相邻的物理上也相邻；而链表不一定，它是用指针来描述元素之间的关系。</p>
</li>
<li>
<p>顺序表插入和删除要移动大量元素；链表只需修改指针(c语言)即可</p>
</li>
</ol>
<p>另外：随机存取就是直接存取，可以通过下标直接访问的那种数据结构，与存储位置无关，例如数组。非随机存取就是顺序存取了，不能通过下标访问了，只能按照存储顺序存取，与存储位置有关，例如链表。</p>
<p>顺序存取就是存取第N个数据时，必须先访问前（N-1）个数据 （list），随机存取就是存取第N个数据时，</p>
<p>不需要访问前（N-1）个数据，直接就可以对第N个数据操作 （array）。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[二级（Java 语言程序设计）考试大纲]]></title>
        <id>https://24684623.github.io/post/er-ji-java-yu-yan-cheng-xu-she-ji-kao-shi-da-gang/</id>
        <link href="https://24684623.github.io/post/er-ji-java-yu-yan-cheng-xu-she-ji-kao-shi-da-gang/">
        </link>
        <updated>2021-07-06T13:43:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="二级公共知识">二级公共知识</h2>
<h2 id="基本要求">基本要求</h2>
<p>1.掌握算法的基本概念<br>
2.掌握基本数据结构及其操作。<br>
3.掌握基本排序和查找算法。<br>
4.逐步求精的结构化程序设计方法。<br>
5.软件工程的基本方法，具有初步应用技术进行软件开发的能力。<br>
6.数据库的基本知识，数据库设计有关的方法命令。</p>
<h2 id="考试内容">考试内容</h2>
<h3 id="一-基本数据结构和算法">一 基本数据结构和算法</h3>
<ol>
<li>算法的基本概念；算法复杂度的概念和意义（时间复杂度与空间复杂度）。</li>
<li>数据结构的定义；数据的逻辑结构与存储结构；数据结构的图形表示；线性结构与非线性结构的概念。</li>
<li>线性表的定义；线性表的顺序存储结构及其插入与删除运算。</li>
<li>栈和队列的定义；栈和队列的顺序存储结构及其基本运算。</li>
<li>线性单链表、双向链表与循环链表的结构及其基本运算。</li>
<li>树的基本概念；二叉树的定义及其存储结构；二叉树的前序、中序和后序遍历。</li>
<li>顺序查找与二分法查找算法；基本排序算法（交换类排序，选择类排序，插入类排序）。</li>
</ol>
<h3 id="二-程序设计基础">二 程序设计基础</h3>
<ol>
<li>程序设计方法与风格。</li>
<li>结构化程序设计。</li>
<li>面向对象的程序设计方法，对象，方法，属性及继承与多态性。</li>
</ol>
<h3 id="三-软件工程基础">三 软件工程基础</h3>
<ol>
<li>软件工程基本概念，软件生命周期概念，软件工具与软件开发环境。</li>
<li>结构化分析方法，数据流图，数据字典，软件需求规格说明书。</li>
<li>结构化设计方法，总体设计与详细设计。</li>
<li>软件测试的方法，白盒测试与黑盒测试，测试用例设计，软件测试的实施，单元测试、集成测试和系统测试。</li>
<li>程序的调试，静态调试与动态调试。</li>
</ol>
<h3 id="四-数据库设计基础">四 数据库设计基础</h3>
<ol>
<li>数据库的基本概念：数据库，数据库管理系统，数据库系统。</li>
<li>数据模型，实体联系模型及E-R图，从E-R图导出关系数据模型。</li>
<li>关系代数运算，包括集合运算及选择、投影、连接运算，数据库规范化理论。</li>
<li>数据库设计方法和步骤：需求分析、概念设计、逻辑设计和物理设计的相关策略。</li>
</ol>
<h2 id="java-语言程序设计">Java 语言程序设计</h2>
<h3 id="基本要求-2">基本要求</h3>
<ol>
<li>掌握Java语言的特点，实现机制和体系结构。</li>
<li>掌握Java语言中面向对象的特性。</li>
<li>掌握Java语言提供的数据类型和结构。</li>
<li>掌握Java语言编程的基本技术。</li>
<li>会编写Java用户界面程序。</li>
<li>会编写Java简单应用程序。</li>
<li>会编写Java小应用程序（Applet）。</li>
<li>了解Java语言的广泛应用。</li>
</ol>
<h3 id="考试内容-2">考试内容</h3>
<h4 id="一-java语言的特点和实现机制">一、Java语言的特点和实现机制</h4>
<h4 id="二-java体系结构">二、Java体系结构</h4>
<ol>
<li>Java程序结构。</li>
<li>Java类库结构。</li>
<li>Java程序开发环境结构。</li>
</ol>
<h4 id="三-java语言中面向对象的特性">三、Java语言中面向对象的特性</h4>
<ol>
<li>面向对象编程的基本概念和特征。</li>
<li>类的基本组成和使用。</li>
<li>对象的生成、使用和删除。</li>
<li>包与接口。</li>
<li>Java类库的常用类和接口。</li>
</ol>
<h4 id="四-java简单数据类型及运算">四、Java简单数据类型及运算</h4>
<ol>
<li>变量和常量。</li>
<li>基本数据类型及转换。</li>
<li>Java类库中对基本数据类型的类包装。</li>
<li>运算符和表达式运算。</li>
<li>字符串和数组。</li>
</ol>
<h4 id="五-java语言的基本语句">五、Java语言的基本语句</h4>
<ol>
<li>条件语句。</li>
<li>循环语句。</li>
<li>注释语句。</li>
<li>异常处理语句。</li>
<li>表达式语句。</li>
</ol>
<h4 id="六-java编程技术基础">六、Java编程技术基础</h4>
<ol>
<li>输入输出流及文件操作。</li>
<li>线程的概念和使用。</li>
<li>程序的同步与共享。</li>
<li>Java语言的继承、多态和高级特性。</li>
<li>异常处理和断言概念。</li>
<li>Java语言的集合（Collections）框架和泛型概念。</li>
</ol>
<h4 id="七-编写用户界面程序基础">七、编写用户界面程序基础</h4>
<ol>
<li>用AWT编写图形用户界面的基本技术。</li>
<li>用Swing编写图形用户界面的特点。</li>
<li>Swing的事件处理机制。</li>
</ol>
<h4 id="八-编写小应用程序applet基础">八、编写小应用程序（Applet）基础</h4>
<ol>
<li>Applet类的API基本知识。</li>
<li>Applet编写步骤及特点。</li>
<li>基于AWT和Swing编写用户界面。</li>
<li>Applet的多媒体支持和通信。</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[菜鸟的成长印记]]></title>
        <id>https://24684623.github.io/post/ji-lu-cai-niao-de-cheng-chang-shi/</id>
        <link href="https://24684623.github.io/post/ji-lu-cai-niao-de-cheng-chang-shi/">
        </link>
        <updated>2021-07-06T13:08:53.000Z</updated>
        <content type="html"><![CDATA[<p>It’s a great pleasure for me to have this opportunity for the interview.</p>
<p>I hope I can make a good performance today.</p>
<p>I have always enjoyed working with computers,so it was an easy<br>
decision to major in Information and Computing Science at</p>
<p>I spent most of my time on study, and I want passed CET4<br>
during my college years. And I have acquired basic knowledge of my major.</p>
<p>It is my long cherished dream to be a software engineer and<br>
I am eager to get an opportunity to fully play my ability.<br>
I think I'm a good team player and a person of great honesty to others.</p>
<p>That’s  all. Thank you for giving me the chance.</p>
]]></content>
    </entry>
</feed>